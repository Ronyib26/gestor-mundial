# En dev se usa el proxy de Vite a localhost:4000, no es necesario.
# En producción Vercel sirve front y API en el mismo dominio, tampoco.
# Solo si quieres apuntar a una API distinta:
# VITE_API_URL=https://mi-otra-api.com/api
