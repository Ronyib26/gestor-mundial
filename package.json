{
  "name": "gestor-mundial",
  "version": "1.0.0",
  "private": true,
  "description": "Gestor del Mundial: CRUD de equipos y grupos + distribución aleatoria",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
    "build": "pnpm --filter client build && pnpm --filter server build",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "@tailwindcss/oxide"
    ]
  }
}
